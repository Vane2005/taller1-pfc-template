package taller

class MaxMinList() {
    @ tailrec
    def maxList( l : List [ Int ]) : Int = {
        def maxListAux(l : List [Int] , max: Int): Int = {
        
        }
    
    }
    @ tailrec
    def minList( l : List [ Int ]) : Int = {
        def minListAux( l : List [ Int ] , max: Int): Int = {
        
        }
    
    }
}